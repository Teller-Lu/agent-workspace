# === S 级文件（秘密级，不可入库）===
# 所有标记为 S 级的文件/目录必须在此列出，确保涉密材料永不提交
# S 级 = 拥有方 AI 可读写，其他 AI 不可读
# 示例（取消注释并替换为实际 S 级路径）：
# Codex/Standard/<机密目录>/
# Claude/confidential/

# === 密钥与配置（含 token，不入库）===
**/automation.conf
**/.codex/config.toml
**/.codex/hooks.json

# === Claude Code 本地审批记录 / 会话缓存（绝不入库）===
# settings.local.json 是用户点"总是允许"累积的白名单，里面有本机绝对路径、邮箱、
# 常访问域名（= 个人研究画像），属敏感信息。
# 必须带 **/ 前缀：每个子目录的 .claude/ 都会各自生成一份；不带 ** 只匹配根级那一份，
# 子目录的全部漏网。（保留 settings.json 和 agents/ —— 那些是要入库的制度文件。）
**/.claude/settings.local.json
**/.claude/sessions/
**/.claude/cache/
**/.claude/logs/
**/.claude/projects/
**/.claude/shell-snapshots/
**/.claude/todos/
**/.claude/statsig/

# === 审计日志（高频追加，按需入库或忽略）===
# 审计日志.jsonl  # 如需入库则注释此行

# === 备份 ===
**/.backups/

# === 临时文件 ===
**/.digests/
**/__pycache__/
*.pyc
*.tmp

# === 运行日志（Automation 高频追加，不入库）===
*.log
# 待同步文档（临时交接文件，不入库）
待同步.md
# 会话交接（AWS 管理会话的本地交接便签，含本机路径/commit hash，不入库）
会话交接.md
